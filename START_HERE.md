# START_HERE.md — 진입점 (홈페이지 제작소)

> **뭐냐:** 이 제작소의 «문». 여기서 앱을 고칠지, 어느 홈페이지를 열지 고른다.
> **언제 읽나:** 이 폴더에서 뭘 하든 **가장 먼저**. 한 장이라 싸다.
> **안 담는 것:** 내용 전부 — 이 문서는 «목차»지 «본문»이 아니다.

---

## 이게 뭐냐

**홈페이지 제작소.** 두 덩이다.

| 덩이 | 어디 | 뭐냐 |
|---|---|---|
| **앱** | [`app/`](app/) | Electron 데스크톱 앱. Next.js 홈페이지 리포를 열어 «피그마처럼» 보고 고친다 |
| **템플릿** | [`templates/`](templates/) | 「새 홈페이지」가 복사해 가는 뼈대. 수산나와 같은 축(Next 16 · React 19 · Tailwind 4 · TS · Cloudflare) |
| **사이트** | `sites/` | 앱이 GitHub 에서 가져오거나 템플릿으로 만든 홈페이지 리포들이 놓이는 곳. **각각 자기 git 을 가진다** |

---

## 앱이 하는 일 (한 화면)

```
┌ 툴바: 프로젝트 · 페이지 ▾ · [선택][손][낙서] 📱 📟 🖥 ⛶ · − 75% + · ● localhost:3000 ▶■ ↻ ↗ </> >_ ┐
│ 레이어 │                                                        │ 디자인  Claude  코드  Git │
│ 페이지 │        캔버스 — 홈페이지가 기기 폭으로 떠 있고            │                          │
│ 토큰   │        클릭하면 파란 선택 상자 + 초록(안쪽)·주황(바깥)     │  고른 요소의 글자·채우기· │
│        │        Alt+호버 = 빨간 거리 선                            │  간격·배치·테두리·효과    │
│        │                                                        │  [코드에 적용] [Claude]  │
└────────┴────────────────────────────────────────────────────────┴──────────────────────────┘
```

- **레이어** = DOM 트리 + **React 컴포넌트 이름**(파란 글씨). 클릭하면 캔버스에서 선택된다.
- **디자인** 값을 만지면 화면에 **즉시** 먹고, 「코드에 적용」을 눌러야 소스(Tailwind 클래스)가 바뀐다.
  소스에서 그 요소가 **유일하게** 안 잡히면 안 고치고 「Claude 에게 맡겨라」로 돌려준다.
- **Claude** 탭 = 선택 요소 + 첨부 그림(끌어놓기·붙여넣기·파일)이 프롬프트 앞에 붙어 `claude -p` 로 간다. 파일 도구만, cwd 는 그 리포.
  🔴 **돈이 나가는지 아닌지를 항상 표시한다** — 구독(OAuth)이면 「구독 max」와 「토큰 $1.41 어치 (청구 아님)」,
  API 키면 「API 키 · 청구됨」. `total_cost_usd` 는 구독으로 써도 오는 «환산값»이라, 금액만 보여주면 청구로 읽힌다.
- **코드** 탭 = 이 요소가 소스 어느 줄인지 (정확히 일치 / 부분 / 추정 을 구분해 말한다).
- **Git** 탭 = 바뀐 파일·커밋·push·pull·기록. **push 가 곧 배포**다(리포에 Cloudflare Workers Builds 가 걸려 있으면).
- **토큰** = `globals.css` 의 `@theme` 변수. 색을 바꾸면 사이트 전체가 따라온다.
- 🔴 **손으로(H)** = **PPT·포토샵처럼 잡아서 옮기고 키우고 돌린다.** 모서리 8개로 크기(Shift 비율 유지),
  안쪽을 끌면 이동, 위 동그라미로 회전(Shift 15°), 방향키 1px·Shift 10px. 부모·형제 가장자리에 **붙고**(Alt 로 해제),
  선택 상자도 같이 기울어진다.
  🔴 **`position:absolute` 를 박지 않는다** — 이동은 `translate-x-[82px]`(흐름 유지·겹침 가능) 또는
  **밀기 모드**의 `ml-[40px]`(주변을 실제로 민다), 크기는 `w-[..] h-[..]`, 회전은 `rotate-[49deg]`.
  끌어 놓은 결과는 「코드에 적용」이 그대로 소스에 적는다.
- 🔴 **함께 고르기 · 맞추기 · 복제** (PPT 의 «정렬» 메뉴) — **Shift+클릭**(캔버스에서도, 레이어 패널에서도)으로 여러 개를 고르면
  디자인 패널에 **맞추기 6**(왼쪽·가운데·오른쪽·위·중간·아래)과 **나누기 2**(가로·세로 간격 고르게)가 뜬다.
  **기준은 마지막에 고른 것**(파란 실선, 나머지는 점선) — PPT 와 같다. **Ctrl+D 로 복제.**
  🔴 **두 가지 정직한 한계:** ① 맞추기는 «눈으로» 맞춘다(각자 `translate`). 제대로는 부모를 `flex` 로 바꾸는 거고
  그건 「Claude」 버튼이 한다. ② **복제는 화면에만 생긴다** — 우리 되찾기는 className 치환이라 JSX 를 복사해 넣지 못한다.
  코드에 넣으려면 Claude 에게 보내야 하고, 앱이 그 말을 요청문 초안으로 미리 채워 준다.
  ⚠️ 화면에 안 보이는 요소(`skip-link` 같은 접근성 링크)는 맞추기에서 자동으로 빠진다 — 안 그러면 10,199px 이 튀어나온다.
- 🔴 **낙서(D)** = 화면 위에 손으로 그린다. **핵심은 번호 핀 ①②③** — 찍으면 앱이 그 자리의 요소를
  webview 에 물어 «무엇인지»까지 같이 붙잡고, 옆에 적은 메모가 그대로 요청문 초안이 된다.
  「Claude 에게」를 누르면 **화면 + 낙서가 PNG 한 장**으로 구워져 첨부되고, 프롬프트에는
  `① 「이 메뉴를 더 크게」 — <h1 class="wordmark…">` 처럼 **정답이 적혀** 나간다.
  그림만 보내면 「빨간 동그라미 친 그거」를 Claude 가 다시 추측해야 하고, 그 추측은 조용히 틀린다.
  도구: 핀(1) 펜(2) 화살표(3) 사각형(4) 동그라미(5) 형광펜(6) 글(7) · Ctrl+Z 되돌리기.

---

## 열어 둔 홈페이지

| # | 홈페이지 | 어디 | 상태 | 비고 |
|---|---|---|---|---|
| 1 | **수산나디자인** | `C:\Users\fivep\Susanna` (sites/ 밖) | 🟢 앱으로 열어서 선택·되찾기까지 확인 (2026-09-12) | 실제 운영 중. push 는 곧 배포 — 확인하고 눌러라 |
| 2 | **DeGaJa 홈페이지** | `C:\Users\fivep\DeGaJa_Agent\web` (sites/ 밖) | 🟢 낙서·핀까지 실측 (2026-09-12) | 실제 운영 중(Cloudflare). 고치기 전에 그 폴더 `CLAUDE.md` 를 읽어라 — 지뢰가 많다 |

**새 홈페이지를 만들거나 가져오면 이 표에 한 줄 넣어라.** 등록 안 하면 다음 세션이 못 찾는다.

---

## 이 제작소의 규칙

- 🔴 **눈으로 확인한 것만 ✅.** 앱은 자기 화면을 찍을 수 있다(`HM_AUTOTEST`). 「컴파일됐다」는 ✅ 가 아니다.
- 🔴 **대상 리포를 오염시키지 마라.** 앱이 리포에 남기는 건 `.homepage-maker/`(첨부 그림, gitignore 에 자동 등록) 하나뿐이다.
  되찾기는 grep 이지 플러그인이 아니다.
- 🔴 **되돌릴 수 없는 건 사람이 한다.** push·발행·도메인·결제. 앱 안의 Claude 는 커밋도 안 한다.
- 🔴 **Claude 에게 주는 권한은 파일 도구까지다.** Bash 는 사람이 토글로 켠 턴에만. `--add-dir` 없음.
- **레퍼런스는 «말»이 아니라 «그림»으로.** 「이 느낌으로」는 스크린샷을 붙여야 Claude 가 본다.
- **기술 축은 수산나와 같다.** 새 축을 들이기 전에 수산나 리포의 `docs/ARCHITECTURE.md` 를 본다 — 이미 푼 문제가 많다.

---

## 이 폴더는 «독립»이다

`DeGaJa_Agent` 리포는 이 제작소를 **포인터 한 줄**로만 안다. 필요한 건 전부 이 폴더 안에 있어야 한다.

| 폴더 | 뭐냐 | 여기랑 무슨 관계 |
|---|---|---|
| `C:\Users\fivep\Susanna` | 수산나디자인 실제 홈페이지 리포 | 이 앱이 **여는** 대상. 기술 축의 원본 |
| `C:\Users\fivep\GAMEMAKER` | 게임 제작소 | 골격(안내판·START_HERE·HANDOVER·SCHEMA)을 여기서 빌려 왔다 |
| `C:\Users\fivep\DeGaJa_Agent` | 개인 비서 하네스 | 여기를 **안 담는다**. 포인터만 |

✅ **git 이 걸렸다** — https://github.com/Gachon-junghyun/HomepageMaker (2026-09-12). `sites/` 는 올라가지 않는다(각자 git 이 있고 남의 홈페이지다).

---

## 변경 이력
- 2026-09-12 — 최초 작성. 앱 v0.1 · 템플릿 · 수산나 등록.
