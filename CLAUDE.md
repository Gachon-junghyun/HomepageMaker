# CLAUDE.md — 이 폴더에 들어온 에이전트에게

> **뭐냐:** 이 폴더에서 일을 시작하는 에이전트가 **가장 먼저·자동으로** 읽는 한 장.
> **언제 읽나:** 자동이다. 세션 시작 때 하네스가 읽힌다.
> **안 담는 것:** 내용 전부. 이건 «문 앞의 안내판»이지 문서가 아니다 — 본문은 아래 둘에 있다.

---

## 먼저 읽어라 (이 순서로)

1. 🔴 **[`START_HERE.md`](START_HERE.md)** — 목차·규칙·사이트 목록. 한 장이라 싸다. **무조건 읽는다.**
2. **[`HANDOVER.md`](HANDOVER.md)** — 지금 뭐가 되고 뭐가 안 되나, 밟은 지뢰, 다음에 할 일.
3. **[`SCHEMA.md`](SCHEMA.md)** — 앱의 트리 구조. 🔴 **뭔가 «추가»하려는 거면 이게 먼저다** — 어디에 붙일지가 거기 있다.

---

## 이 폴더가 뭐냐 (한 문단)

**홈페이지 제작소다.** `GAMEMAKER` 가 게임을 만드는 곳이듯, 여기는 **홈페이지를 만들고 고치는 데스크톱 앱**(`app/`)과
그 앱이 여는 **홈페이지 리포들**(`sites/`)이 산다. 앱은 Next.js 홈페이지 리포를 열어 화면을 띄우고,
요소를 클릭해 PPT 처럼 색·글자·간격을 바꾸고, 그림을 들고 와 Claude 에게 「이렇게 해줘」 하면 코드를 고친다.
기술 축은 **수산나디자인 홈페이지(`C:\Users\fivep\Susanna`)와 같다** — Next 16 · React 19 · Tailwind 4 · TS · Cloudflare.

🔴 **`Susanna` 와 헷갈리지 마라.** 저긴 «수산나디자인의 실제 홈페이지 리포»고, 여긴 «홈페이지를 만드는 도구»다.
수산나 홈페이지를 고칠 때는 이 앱으로 그 리포를 **열면** 된다. 여기에 수산나 코드를 복사하지 마라.

---

## 🔴 손대기 전에 알아야 할 것 셋

- **눈으로 확인한 것만 ✅.** 앱은 `HM_AUTOTEST` 로 자기 화면을 찍는다(`app/src/main/autotest.ts`). 「빌드됐다」는 ✅ 가 아니다.
- **리포를 건드리지 않고 되찾는다.** 화면의 요소 → 소스 줄은 **className 문자열 grep** 이다. 바벨 플러그인·소스맵을 대상 리포에 심지 마라 — 그 순간 «아무 Next 리포나 열 수 있다»가 깨진다.
- **되돌릴 수 없는 건 사람이 한다.** push(=배포)·발행. 앱의 Claude 는 파일 도구만 쥐고(Bash 는 사람이 토글), cwd 밖을 못 본다.

---

## 실행

```
start.cmd                    ← 빌드하고 띄운다 (처음엔 app/ 에서 npm install 이 먼저다)
app: npm run dev             ← 개발 모드 (HMR)
app: HM_AUTOTEST=<리포> npx electron .   ← 자가 검증. %TEMP%\hm-autotest\ 에 스크린샷·로그
```

---

## 변경 이력
- 2026-09-12 — 최초 작성. 사람이 「GAMEMAKER 처럼 HOMEPAGE_MAKER 를」이라고 해서 같은 골격(안내판·START_HERE·HANDOVER·SCHEMA)으로 세웠다.
