# HANDOVER.md — 지금 뭐가 되고 뭐가 안 되나

> **뭐냐:** 앱의 현재 상태·밟은 지뢰·다음에 할 일. 세션을 이어받는 사람이 두 번째로 읽는 장.
> **언제 읽나:** 앱을 고치기 전에. 「이거 왜 이렇게 돼 있지」가 떠오를 때.
> **안 담는 것:** 구조(→ `SCHEMA.md`) · 규칙(→ `START_HERE.md`) · 피그마에서 뭘 빌렸나(→ `RESEARCH/figma_knowhow.md`).

---

## 30초 요약 (2026-09-12, 낙서까지)

🎨 **낙서가 붙었다.** 화면 위에 손으로 그리고 **번호 핀 ①②③** 을 찍으면, 앱이 그 자리의 요소를
webview 에 물어 «무엇인지»까지 붙잡아 프롬프트에 적어 보낸다 — 드가자 홈페이지에서 실측했고
②가 `<h1 class="wordmark…">` 로 정확히 잡혔다. 핀 옆 메모는 그대로 요청문 초안이 된다.
**두 번째 홈페이지(드가자 `web/`)도 열어서 돌렸다.**

## 30초 요약 (앱 본체)

앱 v0.1 이 **돈다.** 수산나 리포를 열어 dev 서버 → 캔버스 → 클릭 선택 → 라이브 스타일 → **소스 줄 되찾기**(`components/HeroSlider.tsx:250`)까지
자가 검증 스크린샷으로 확인했다. 템플릿으로 새 홈페이지를 만드는 흐름(복사 → git init → npm install → dev → 렌더)도 확인했다.
**Claude 호출 배관은 끝까지 돌지만 이 PC 의 `claude` CLI 로그인이 만료돼 실제 편집은 못 봤다** — 터미널에서 `claude` → `/login` 이 먼저다.

## 지금 상태

| 기능 | 상태 | 근거 |
|---|---|---|
| 폴더 열기 / GitHub clone / 템플릿으로 새로 만들기 | ✅ / ⚠️ / ✅ | clone 은 코드만 있고 실제 URL 로 안 돌려봤다 |
| dev 서버 띄우기·포트 읽기·로그 서랍 | ✅ | 3000 이 차 있으면 3001 을 읽는 것도 봤다 |
| 캔버스: 기기 폭·줌·선택 도구 | ✅ | 데스크톱 1440·모바일 390 스크린샷 |
| 호버·선택 상자·박스모델 칠하기·Alt 거리 재기 | ✅ / ✅ / ✅ / ⚠️ | Alt 측정은 코드만, 손으로 안 눌러봤다 |
| 레이어 트리 + React 컴포넌트 이름 | ✅ | 수산나에서 SiteChrome·HeroSlider·QuickQuoteForm 이 잡혔다 |
| 디자인 패널 라이브 적용 | ✅ | 배경색 주황 스크린샷 |
| **코드에 적용**(className 되찾기 → Tailwind 클래스 재작성, BOM 보존) | ✅ | `source.ts` 단위 테스트 (리포 복사본) |
| 글 바꾸기 | ⚠️ | 유일할 때만 고친다. 조합된 글은 거부 → Claude |
| 코드 탭(줄 찾기·스니펫·VS Code 열기) | ✅ / ✅ / ⚠️ | `code -g` 는 안 눌러봤다 |
| Claude 탭(선택·첨부·스트리밍·세션 이어가기·Bash 토글) | ⚠️ | stream-json 파싱·에러 표시까지 확인. **편집은 로그인 뒤 확인** |
| Git 탭(상태·커밋·push·pull·diff·기록·remote 연결) | ✅(읽기) / ⚠️(쓰기) | 수산나 로그 40건이 떴다. 커밋·push 는 안 눌렀다 |
| 토큰 탭(`@theme` 읽기·쓰기) | ✅ / ⚠️ | 수산나 브랜드 색 14개 읽힘. 쓰기는 안 눌렀다 |
| 페이지 탭 + 「페이지 추가(Claude)」 | ✅ / ⚠️ | |
| 패키징(설치 파일) | ❌ | electron-builder 안 붙임. `start.cmd` 로 띄운다 |
| 🎨 **낙서**: 핀·펜·화살표·사각형·동그라미·형광펜·글 | ✅ | 드가자 홈에서 스크린샷으로 확인 |
| 🎨 핀이 «그 자리의 요소»를 붙잡기 | ✅ | `h1.wordmark` 를 정확히 잡았다 |
| 🎨 화면+낙서 PNG 굽기 → 첨부 · 핀 메모 → 요청문 초안 | ✅ | 구운 PNG·프롬프트 전문 확인 |
| 🎨 되돌리기·전부 지우기·색·굵기 | ✅ / ⚠️ | Ctrl+Z 는 코드만, 손으로 안 눌러봤다 |

## 밟은 지뢰 (전부 2026-09-12 실측)

- 🔴 **`webview { display:block }` 을 주면 게스트가 1440×150 으로 찌그러진다.** Electron 이 내부적으로 flex 를 쓴다.
  증상은 「페이지 아래가 하얗다 + `elementFromPoint` 가 null」. `styles.css` 에 주석으로 박아뒀다.
- 🔴 **webview 안 React fiber 를 읽으려면 `contextIsolation: false`.** isolated world 에서는 DOM 노드의 `__reactFiber$` 확장 속성이 안 보인다.
  그래서 `will-attach-webview` 에서 끈다. 페이지 스크립트에 `ipcRenderer` 를 노출하진 않는다.
- 🔴 **앱을 강제로 죽이면(`taskkill electron`) `next dev` 가 고아로 남아 3000 을 잡고 있다.** 정상 종료(창 닫기)는 `taskkill /T` 로 같이 죽인다.
  개발 중 «3001 로 뜬다»면 고아가 있는 거다: `netstat -ano | findstr :3000` → PID 죽이기.
  ⚠️ **Git Bash 에서 `taskkill /IM electron.exe` 는 조용히 실패했다** — 31개가 살아 있었다. PowerShell `Get-Process electron | Stop-Process -Force` 가 확실하다.
- **`npm install` 이 postinstall 을 건너뛰어 Electron 바이너리가 없었다.** `node node_modules/electron/install.js` 로 받았다.
- **`@vitejs/plugin-react@6` 은 vite 8 을 요구한다.** electron-vite 5 는 vite 7 까지라 plugin-react 5 로 박았다.
- **`@opennextjs/cloudflare@1.20.6` 은 `next >=16.3.3` 을 요구한다.** 수산나의 `next 16.2.11` 을 그대로 템플릿에 넣었더니 ERESOLVE. 템플릿은 `^16.3.5`.
- **Electron 44 에는 `clipboard.readImage` 가 없다**(W3C 식 `clipboard.read()` 만). 그림 붙여넣기는 렌더러의 `navigator.clipboard.read()` 로 한다.
- **주석 안의 `app/**/page.tsx` 가 블록 주석을 닫아 버린다.** (`**/`) 문서에 쓸 땐 `app 폴더의 page.tsx` 로.
- **Bash 툴 heredoc 이 한글 JSX 파일에서 두 번 깨졌다.** 원인을 못 잡았다(BOM 문자·백틱 의심). 소스 파일은 Write 도구로 쓴다.
- **클릭을 `dispatchEvent` 로 흉내 내면 `elementFromPoint` 가 못 잡는다.** 자가 검증은 `webview.sendInputEvent` 로 진짜 클릭을 넣는다.

## 낙서에서 밟은 지뢰 (2026-09-12 실측)

- 🔴 **`capturePage()` 가 «직전 프레임»을 준다.** 낙서를 그린 직후 찍으면 **낙서가 없는 그림**이 나온다.
  그래서 「안 그려졌다」로 두 번 오진했다 — DOM 은 멀쩡했다. 자가 검증의 `shot()` 은 이제
  `requestAnimationFrame` 두 번 + 120ms 를 기다린 뒤 찍는다. **구운 첨부 PNG 는 처음부터 정상이었다.**
- 🔴 **스크롤되는 컨테이너 안의 `absolute bottom-4` 는 «스크롤된 콘텐츠»의 바닥에 붙는다.**
  캔버스가 `overflow-auto` 라 낙서 툴바가 화면 밖으로 내려가 있었다. `fixed` + 좌우 패널 폭 보정으로 고쳤다.
- 🔴 **`.gitignore` 등록이 «파일 첨부» 경로에만 있었다.** 낙서(바이트 저장)는 그 처리가 빠져서
  드가자 리포에 **6장 2.3MB 가 추적되지 않은 채** `git status` 에 떴다. 두 경로가 `refsDir()` 하나를 쓰도록 합쳤다.
  → 같은 일이 재발하지 않게 **그림을 두는 함수는 이것 하나뿐**이어야 한다.
- 🔴 **렌더러에서 `file:///` 이미지는 안 뜬다** (조용히 빈 칸). `hm-file://` 전용 스킴을 등록하고 CSP 에 넣었다.
  읽어 주는 건 «이미지 확장자 + 실제로 있는 파일»뿐이다.
- **dev 서버가 첫 컴파일 중이면 오버레이(preload)가 안 붙은 채 페이지만 뜨는 일이 있다.**
  그러면 클릭도 핀도 조용히 안 먹는다 → `ready` 가 4초 안에 안 오면 한 번 되살린다(최대 2회).
- **자가 검증의 합성 포인터는 `setPointerCapture` 에서 throw 한다.** 삼키게 했다(실사용엔 없는 일).

## 알고 남긴 한계

- **서버 컴포넌트는 레이어에 이름이 안 뜬다.** RSC 는 클라이언트 fiber 에 함수가 없다. 템플릿 홈의 `Section` 이 그렇다. 클라이언트 컴포넌트(`"use client"`)만 잡힌다.
- **되찾기는 className 이 «유일»할 때만 고친다.** `cn()`·조건부·템플릿 리터럴은 「추정」으로 파일만 알려주고 Claude 에게 넘긴다. 추측해서 고치지 않는 게 설계다.
- **Tailwind 재작성은 base 클래스만 건드린다.** `md:`·`hover:` 변형은 안 지운다. 반응형 값이 이기고 있으면 「적용했는데 안 바뀐다」로 보일 수 있다 — 코드 탭에서 확인.
- **`LinkComponent`** 는 Next 의 `Link` 내부 이름이다. 그대로 보인다.
- **낙서는 «지금 보이는 화면»만 찍는다.** 스크롤 밖은 안 들어간다 — 아래쪽을 설명하려면 스크롤한 뒤 찍어라.
- **핀은 좌표로 요소를 찾는다**(`elementFromPoint`). 큰 컨테이너 위를 찍으면 그 컨테이너가 잡힌다 —
  정확히 집으려면 **선택 도구로 고른 뒤** 낙서로 넘어가는 게 낫다(선택도 같이 프롬프트에 간다).
- **templates/ 경로는 개발 실행 기준**(`app.getAppPath()/../templates`). 패키징하면 `extraResources` 로 옮겨야 한다.

## 다음에 할 일

1. `claude` 로그인 뒤 **Claude 편집 한 바퀴 실측** — 선택 + 그림 첨부 + 「이렇게」 → 파일이 바뀌고 HMR 로 화면이 따라오는지.
2. Git 탭 커밋·push 를 **템플릿 사이트**로 한 번 눌러본다 (수산나로 하지 마라 — 그건 배포다).
3. Alt 측정·방향키 탐색·「같은 요소 재클릭=부모」를 손으로.
4. 반응형: 디자인 패널에서 «지금 기기 폭의 변형(`md:`)으로 적용» 옵션.
5. 히스토리: 코드 적용마다 자동 체크포인트(`git stash`가 아니라 커밋 없이 되돌릴 방법) — 지금은 Git 탭의 «파일 되돌리기»뿐.
6. 패키징(electron-builder) + `templates/` 를 `extraResources` 로.
7. 낙서: 스크롤하며 여러 장 찍어 한 번에 보내기 · 핀을 끌어서 옮기기 · 핀만 남기고 획 지우기.

## 변경 이력
- 2026-09-12 (이어서) — 낙서·번호 핀 추가. 드가자 홈페이지(`web/`)를 두 번째 대상으로 등록.
- 2026-09-12 — 최초 작성. v0.1.
